// shoppingListService.js
const getShoppingList = () => {
    // Fetch shopping list
  };
  
  const addItem = (itemData) => {
    // Add item to list
  };
  
  export default { getShoppingList, addItem };