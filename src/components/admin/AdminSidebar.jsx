import React from 'react';

const AdminSidebar = () => {
  return (
    <div>
      <h2>Menu Admin</h2>
      {/* Liens vers les pages Admin: Tableau de bord, Gestion des utilisateurs, Modération, etc. */}
    </div>
  );
};

export default AdminSidebar;